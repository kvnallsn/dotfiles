export PATH=$PATH:~/.local/bin

# pnpm
export PNPM_HOME="/home/coder/.local/share/pnpm"
export PATH="$PNPM_HOME:$PATH"
# pnpm end

alias ls='exa'
alias vim='nvim'
alias emacs='nvim'

alias ll='ls -l'
alias lla='ls -la'
alias llah='ls -lah'
